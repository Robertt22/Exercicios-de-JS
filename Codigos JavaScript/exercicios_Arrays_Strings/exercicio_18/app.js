let frase ="  logica de programacao  ";
let remover = frase.trim("")
let separar = remover.split("");
let numero_total_de_letras = 0;

for(let i = 0; i < separar.length; i++){
numero_total_de_letras = i

}console.log(`A frase original é "${frase}", seu número de letras é de: ${frase.length}. O número total de letras da palavra modificada é de: ${numero_total_de_letras}`)