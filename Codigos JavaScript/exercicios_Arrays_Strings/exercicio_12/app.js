let palavras = [ "sol", "lua", "estrela", "nuvem", "chuva", "vento", "neve", "raio", "arco", "tempestade"
]; 

  for(let i = 0; i < palavras.length; i++){
   if( palavras[i].split("").length % 2 == 0){
console.log(` ${palavras[i]}`)
   }else {}
  }console.log(`  .`)