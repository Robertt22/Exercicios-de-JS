let palavra = "CALANGUINHO";
let converter = palavra.toLowerCase();
let nova_palavra ="";
let metade =Math.ceil(converter.length/2);

nova_palavra = converter.substring(0,6)
console.log(nova_palavra)
